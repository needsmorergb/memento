---
phase: 01-same-day-edit-video-control
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - artifacts/api-server/src/routes/media.ts
  - artifacts/memento-mobile/app/(tabs)/camera.tsx
  - artifacts/memento-web/src/pages/event-join.tsx
autonomous: true
requirements: [VIDEO-03]
must_haves:
  truths:
    - "The media confirm endpoint accepts an optional capturedAt and persists it to media_items.captured_at"
    - "Mobile capture sends capturedAt taken at shutter/record/voice-start for photo, video, and voice uploads"
    - "Web guest join sends capturedAt at file selection"
    - "Omitting capturedAt still works (backward compatible) — server falls back to createdAt"
  artifacts:
    - path: "artifacts/api-server/src/routes/media.ts"
      provides: "Confirm handler reads capturedAt from body and inserts it (optional)"
      contains: "capturedAt"
    - path: "artifacts/memento-mobile/app/(tabs)/camera.tsx"
      provides: "Capture-time timestamp threaded into confirmUpload for photo/video/voice"
      contains: "capturedAt"
    - path: "artifacts/memento-web/src/pages/event-join.tsx"
      provides: "capturedAt sent at confirm on web guest path"
      contains: "capturedAt"
  key_links:
    - from: "client confirm body (camera.tsx / event-join.tsx)"
      to: "media.ts confirm handler"
      via: "useConfirmMediaUpload data.capturedAt"
      pattern: "capturedAt"
    - from: "media.ts confirm insert"
      to: "media_items.captured_at column"
      via: "db.insert(mediaItemsTable).values({ capturedAt })"
      pattern: "capturedAt"
---

<objective>
Complete the capture-time half of VIDEO-03 across the stack: the API confirm handler accepts and persists the optional `capturedAt`, and both clients send it — captured at the actual shutter/record/voice-start moment on mobile, and at file selection on web. With Plan 02's worker now ordering by `capturedAt ?? createdAt`, this slice makes voice notes (and clips) land at the moment they were captured. Runs in Wave 2 in parallel with Plan 02 — no shared files (media.ts + clients here; worker/events.ts there).

Purpose: Make capture time real end-to-end — the column (Plan 01) and worker ordering (Plan 02) are inert until clients actually send a capture timestamp.
Output: Edited `media.ts` confirm handler + mobile `camera.tsx` + web `event-join.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-same-day-edit-video-control/01-RESEARCH.md
@.planning/phases/01-same-day-edit-video-control/01-PATTERNS.md
@.planning/phases/01-same-day-edit-video-control/01-01-SUMMARY.md

<interfaces>
<!-- Extracted analogs -->
- media.ts confirm handler :98-198. Body destructure :125-139 (objectPath, mediaType, fileName, fileSizeBytes, durationSeconds, thumbnailPath). Insert values :163-176 (db.insert(mediaItemsTable).values({ eventId, guestId, uploaderId, mediaType, objectPath, fileName, fileSizeBytes, durationSeconds, thumbnailPath }).returning()). 201 response :180-193. Path-reuse 409 at :156.
- Generated `useConfirmMediaUpload` hook (api.ts:1409); request type is ConfirmMediaUploadRequest (now has optional capturedAt after Plan 01). Generated zod validates date-time with useDates:true.
- Mobile camera.tsx doUpload/confirm :78-141; confirmUpload.mutate at ~:96-128. Capture moments: photo at handleTakePhoto (:157), video from recordingStartRef (:178), voice from voiceStartRef (:224).
- Web event-join.tsx confirmUpload.mutate (:96); hook at :227. File selection is the capturedAt source (file.lastModified or new Date()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API confirm handler accepts and persists optional capturedAt</name>
  <read_first>
    - artifacts/api-server/src/routes/media.ts (the file being modified — :98-198, esp. destructure :125-139 and insert :163-176)
    - .planning/phases/01-same-day-edit-video-control/01-PATTERNS.md (§ media.ts — destructure + insert additions)
    - .planning/phases/01-same-day-edit-video-control/01-01-SUMMARY.md (confirm capturedAt is in ConfirmMediaUploadRequest and not required)
  </read_first>
  <action>
    Edit `artifacts/api-server/src/routes/media.ts` only. Add `capturedAt` to the confirm body destructure (:125-139), typed `capturedAt?: string`. In the `db.insert(mediaItemsTable).values({...})` call (:163-176), add `capturedAt: capturedAt ? new Date(capturedAt) : undefined` (optional — when omitted, the column stays null and the worker falls back to createdAt). Do not change validation flow, the path-reuse 409, or any other field. Optionally include `capturedAt` in the 201 response object. Keep handler-returns-void and error conventions intact.
  </action>
  <verify>
    <automated>cd D:/apaul/Documents/memento && grep -c "capturedAt" artifacts/api-server/src/routes/media.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "capturedAt" artifacts/api-server/src/routes/media.ts` returns >= 2 (destructure + insert).
    - The insert passes `capturedAt: capturedAt ? new Date(capturedAt) : undefined` (or equivalent null-safe conversion) — never a raw string into the timestamp column.
    - Omitting capturedAt from the body still inserts successfully (column null).
    - `pnpm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Confirm handler reads optional capturedAt, converts to Date, and persists it; backward compatible when absent.</done>
</task>

<task type="auto">
  <name>Task 2: Clients send capturedAt at capture/selection (mobile + web)</name>
  <read_first>
    - artifacts/memento-mobile/app/(tabs)/camera.tsx (the file being modified — doUpload/confirm :78-141; capture moments :157,:178,:224)
    - artifacts/memento-web/src/pages/event-join.tsx (the file being modified — confirmUpload.mutate :96; hook :227)
    - .planning/phases/01-same-day-edit-video-control/01-PATTERNS.md (§ camera.tsx + event-join.tsx — thread capturedAt into confirm data)
    - .planning/phases/01-same-day-edit-video-control/01-RESEARCH.md (Code Examples: client capture timestamp; A6 web source)
  </read_first>
  <action>
    Mobile (`artifacts/memento-mobile/app/(tabs)/camera.tsx`): capture the moment as an ISO string at the actual capture event and thread it through `doUpload`'s params into the `confirmUpload.mutate` `data` object alongside the existing fields. Photo: `new Date().toISOString()` at `handleTakePhoto` (:157). Video: derive from `recordingStartRef.current` (record-start) (:178). Voice: derive from `voiceStartRef.current` (record-start) (:224). Add a `capturedAt` parameter to `doUpload` and include `capturedAt` in the `confirmUpload.mutate({ eventId, data: { ...existing, capturedAt } }, ...)` call. Do not change upload mechanics, presign flow, or the success/error handling.
    Web (`artifacts/memento-web/src/pages/event-join.tsx`): at file selection derive `capturedAt` from `file.lastModified` (as an ISO string) or fall back to `new Date().toISOString()`, and add it to the `confirmUpload.mutate` data (:96). Optional everywhere; null falls back to createdAt server-side.
    Both: the field flows through the regenerated `useConfirmMediaUpload` typed hook (capturedAt is now an optional property on ConfirmMediaUploadRequest) — do not hand-construct fetches.
  </action>
  <verify>
    <automated>cd D:/apaul/Documents/memento && grep -c "capturedAt" "artifacts/memento-mobile/app/(tabs)/camera.tsx" && grep -c "capturedAt" artifacts/memento-web/src/pages/event-join.tsx && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "capturedAt" "artifacts/memento-mobile/app/(tabs)/camera.tsx"` returns >= 3 (photo, video, voice paths thread it) — or >= 1 if threaded through a single shared doUpload param that all three call sites populate; either is acceptable as long as all three capture paths supply a capture-time value.
    - `grep -c "capturedAt" artifacts/memento-web/src/pages/event-join.tsx` returns >= 1.
    - capturedAt is an ISO date-time string passed via the generated `useConfirmMediaUpload` data, not a manual fetch.
    - `pnpm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Mobile sends capture-time for photo/video/voice; web sends selection-time; both via the typed confirm hook, optional and backward compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → media confirm | `capturedAt` is client-supplied and untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | capturedAt confirm field | accept | capturedAt is ordering-only input. It cannot widen access, change ownership, or bypass tier caps — those are resolved server-side at /end from the authenticated host's subscription, independent of capturedAt. A forged value at worst reorders one event's own media. Validated as date-time by generated zod. |
| T-03-02 | Tampering | Express 5 req.params (media confirm) | mitigate | Existing handler already uses String(...) on params; this change touches only the body, not params. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; no install task. |
</threat_model>

<verification>
- Confirm handler persists capturedAt when present; null when absent.
- Mobile sends capture-time for all three capture types; web sends selection-time.
- `pnpm run typecheck` passes.
</verification>

<success_criteria>
- A media item uploaded with capturedAt stores that exact time; omitting it falls back to createdAt.
- Combined with Plan 02's worker ordering, voice notes are positioned by capture time (VIDEO-03 satisfied end-to-end).
</success_criteria>

<output>
Create `.planning/phases/01-same-day-edit-video-control/01-03-SUMMARY.md` when done.
</output>
