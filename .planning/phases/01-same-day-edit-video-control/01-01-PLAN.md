---
phase: 01-same-day-edit-video-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/src/schema/index.ts
  - lib/api-spec/openapi.yaml
autonomous: true
requirements: [VIDEO-01, VIDEO-02, VIDEO-03]
must_haves:
  truths:
    - "Postgres video_job_status enum includes ready_for_review (additive, non-breaking)"
    - "video_jobs has nullable approved_at and superseded_at columns"
    - "media_items has a nullable captured_at column"
    - "Generated React Query hooks include useApproveEventVideo and useRegenerateEventVideo"
    - "VideoJobStatus response schema exposes status (incl. ready_for_review) and approvedAt"
    - "ConfirmMediaUploadRequest accepts optional capturedAt and is NOT in required[]"
  artifacts:
    - path: "lib/db/src/schema/index.ts"
      provides: "ready_for_review enum value; approvedAt/supersededAt on videoJobsTable; capturedAt on mediaItemsTable"
      contains: "ready_for_review"
    - path: "lib/api-spec/openapi.yaml"
      provides: "approve + regenerate paths; extended VideoJobStatus + ConfirmMediaUploadRequest"
      contains: "approveEventVideo"
    - path: "lib/api-client-react/src/generated/api.ts"
      provides: "Generated hooks for the new endpoints (regenerated, not hand-edited)"
      contains: "useApproveEventVideo"
  key_links:
    - from: "lib/db/src/schema/index.ts"
      to: "Postgres video_jobs/media_items tables"
      via: "pnpm --filter @workspace/db run push"
      pattern: "captured_at|approved_at|superseded_at"
    - from: "lib/api-spec/openapi.yaml"
      to: "lib/api-client-react/src/generated/api.ts"
      via: "pnpm --filter @workspace/api-spec run codegen (Orval)"
      pattern: "useApproveEventVideo|useRegenerateEventVideo"
---

<objective>
Lay the spec-first foundation for the entire phase: the additive DB schema changes (review-gate status + timestamps, capture timestamp) and the OpenAPI contract changes (new approve/regenerate endpoints, extended video-status and media-confirm schemas), then regenerate the DB and the typed client. This is a BLOCKING Wave-1 plan — Plans 02, 03, and 04 all consume the columns, enum value, response shape, and generated hooks produced here. It combines the status-enum change (review gate) and the capturedAt change (VIDEO-03) into one schema/codegen pass to avoid a double db-push churn, exactly as the planning context directs.

Purpose: Make the new review-gate states, audit timestamps, capture timestamp, and endpoints exist as first-class, codegen-generated contracts before any handler or UI is written — honoring the repo's mandatory spec-first discipline (edit schema/openapi FIRST, then push/codegen, then restart).
Output: Edited `schema/index.ts` + `openapi.yaml`; pushed DB; regenerated `lib/api-client-react/src/generated/*` and `lib/api-zod/src/generated/*` (NOT hand-edited).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-same-day-edit-video-control/01-CONTEXT.md
@.planning/phases/01-same-day-edit-video-control/01-RESEARCH.md
@.planning/phases/01-same-day-edit-video-control/01-PATTERNS.md

<interfaces>
<!-- Current schema (lib/db/src/schema/index.ts) — extracted; edit in place, additive only -->
- `videoJobStatusEnum = pgEnum("video_job_status", ["pending","processing","completed","failed"])` at ~:28 — ADD "ready_for_review" between "processing" and "completed". NEVER rename/remove an existing value.
- `mediaItemsTable` (:179-195) — nullable timestamp convention already present (`createdAt`/`updatedAt`/`deletedAt`). Columns use snake_case DB names: `timestamp("created_at")`.
- `videoJobsTable` (:208-226) — nullable timestamps `startedAt: timestamp("started_at")`, `completedAt: timestamp("completed_at")` already present; mirror them.
- `insertVideoJobSchema` (:228) `.omit({...})` — already omits server-managed fields (status, completedAt, etc.).
- `insertMediaItemSchema` (:197) `.omit({ id, createdAt, updatedAt, deletedAt })` — capturedAt MUST NOT be added to this omit list (client supplies it).

<!-- Current OpenAPI seams (lib/api-spec/openapi.yaml) — extracted from RESEARCH/PATTERNS -->
- `endEvent` path (:188-207): the analog to copy for new POST paths — `security: [- clerkAuth: []]`, `parameters: [- $ref: "#/components/parameters/eventId"]`, `200 -> $ref VideoJobStatus`, `401 -> Unauthorized`, `404 -> NotFound`.
- `VideoJobStatus` schema (:1099-1129): has `status` enum + nullable `completedAt`. Mirror `completedAt` to add `approvedAt`.
- `ConfirmMediaUploadRequest` schema (:980-996): has optional `fileName`/`durationSeconds`; `required: [objectPath, mediaType]`. Add `capturedAt` as optional — do NOT add to required[].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive DB schema changes (enum value + 3 nullable columns)</name>
  <read_first>
    - lib/db/src/schema/index.ts (the file being modified — read the enum at ~:28, mediaItemsTable :179-204, videoJobsTable :208-239)
    - .planning/phases/01-same-day-edit-video-control/01-PATTERNS.md (§ schema/index.ts — exact lines to add)
    - .planning/phases/01-same-day-edit-video-control/01-RESEARCH.md (§ Recommended Approach: status model; A1/A2 — why additive)
  </read_first>
  <action>
    Edit `lib/db/src/schema/index.ts` only, additively:
    1. Append "ready_for_review" to `videoJobStatusEnum`, positioned between "processing" and "completed" (Postgres ADD VALUE — non-breaking; drizzle-kit push applies it). Do NOT rename or remove "completed"/"failed"/etc — every client `status === "completed"` check must stay valid.
    2. In `mediaItemsTable`, add a nullable `capturedAt: timestamp("captured_at")` mirroring the existing nullable `deletedAt` style (no `.notNull()`, no default). This is client-supplied at confirm (VIDEO-03).
    3. In `videoJobsTable`, add nullable `approvedAt: timestamp("approved_at")` and `supersededAt: timestamp("superseded_at")`, mirroring the existing `startedAt`/`completedAt` nullable-timestamp style.
    4. In `insertVideoJobSchema.omit({...})`, add `approvedAt: true` and `supersededAt: true` (handler-managed, like `completedAt`). Do NOT touch `insertMediaItemSchema` — `capturedAt` must remain accepted there since the client supplies it.
    After editing, run `pnpm --filter @workspace/db run push` to apply the enum ADD VALUE + nullable columns. Inspect push output for the enum-value add and the three column adds; this command requires the Replit-provisioned DATABASE_URL.
  </action>
  <verify>
    <automated>cd D:/apaul/Documents/memento && grep -v '^[[:space:]]*//' lib/db/src/schema/index.ts | grep -c -E "ready_for_review|captured_at|approved_at|superseded_at"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' lib/db/src/schema/index.ts | grep -c -E "ready_for_review|captured_at|approved_at|superseded_at"` returns 4 (one per added identifier, comments excluded).
    - `videoJobStatusEnum` array still contains the original four values unchanged (pending, processing, completed, failed) plus ready_for_review.
    - `pnpm --filter @workspace/db run push` completes without error and reports the enum value + 3 column additions.
    - `insertMediaItemSchema.omit({...})` does NOT list `capturedAt`.
  </acceptance_criteria>
  <done>Schema file has the additive enum value and three nullable columns; DB push applied them; insert schemas adjusted (videoJobs omits approvedAt/supersededAt, mediaItems still accepts capturedAt).</done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI contract — new paths + extended schemas + codegen</name>
  <read_first>
    - lib/api-spec/openapi.yaml (the file being modified — read endEvent :188-207, VideoJobStatus :1099-1129, ConfirmMediaUploadRequest :980-996)
    - .planning/phases/01-same-day-edit-video-control/01-PATTERNS.md (§ openapi.yaml — exact YAML blocks to mirror)
    - .planning/codebase/CONVENTIONS.md (§ Codegen Discipline — indexFiles:false, clean:true, titleTransformer, never hand-edit generated/)
  </read_first>
  <action>
    Edit `lib/api-spec/openapi.yaml` only, then regenerate. Mirror the `endEvent` path block verbatim for two NEW paths placed near the existing video paths:
    - `POST /events/{eventId}/video/approve` with `operationId: approveEventVideo`, `tags: [video]`, `security: [- clerkAuth: []]`, `parameters: [- $ref: "#/components/parameters/eventId"]`. Responses: 200 -> `$ref VideoJobStatus`, 401 -> `$ref Unauthorized`, 404 -> `$ref NotFound`, plus a 409 (conflict) response described as "video not in ready_for_review state".
    - `POST /events/{eventId}/video/regenerate` with `operationId: regenerateEventVideo`, same tags/security/parameters. Responses: 200 -> `$ref VideoJobStatus`, 401 -> Unauthorized, 404 -> NotFound.
    Extend `VideoJobStatus` schema: add `ready_for_review` to the `status` enum list, and add an `approvedAt` field (type string, format date-time, nullable: true) mirroring the existing nullable `completedAt`.
    Extend `ConfirmMediaUploadRequest` schema: add optional `capturedAt` (type string, format date-time) alongside the other optional fields. Do NOT add `capturedAt` to the schema's `required` list (backward compatibility — VIDEO-03 field is optional).
    Then run, in order: `pnpm --filter @workspace/api-spec run codegen` (Orval regenerates hooks + zod and runs typecheck:libs), then `pnpm run typecheck`. NEVER hand-edit anything under `lib/api-client-react/src/generated/` or `lib/api-zod/src/generated/`, and do NOT remove the `indexFiles: false` flag or alter `lib/api-zod/src/index.ts` (single `export * from "./generated/api"` line).
  </action>
  <verify>
    <automated>cd D:/apaul/Documents/memento && grep -c -E "useApproveEventVideo|useRegenerateEventVideo" lib/api-client-react/src/generated/api.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `lib/api-spec/openapi.yaml` contains `operationId: approveEventVideo` and `operationId: regenerateEventVideo`.
    - `VideoJobStatus` `status` enum in openapi.yaml includes `ready_for_review`; an `approvedAt` (nullable date-time) field is present.
    - `ConfirmMediaUploadRequest` has a `capturedAt` property and `capturedAt` does NOT appear in that schema's `required:` list.
    - `grep -c -E "useApproveEventVideo|useRegenerateEventVideo" lib/api-client-react/src/generated/api.ts` returns >= 2 (hooks regenerated).
    - `pnpm run typecheck` exits 0.
    - `lib/api-zod/src/index.ts` remains a single `export * from "./generated/api"` line.
  </acceptance_criteria>
  <done>OpenAPI has both new operationIds and the extended schemas; codegen produced the new hooks and zod; workspace typechecks; generated files were regenerated, never hand-edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → generated client | Contract changes here propagate to both clients via codegen; a mistake (e.g. exposing review fields by default) becomes a client-wide leak. |
| client → DB (captured_at) | `capturedAt` originates from an untrusted client at confirm; schema must keep it nullable/optional and never make it access-control relevant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | VideoJobStatus schema | mitigate | Schema only declares fields; the public-path gating that withholds `videoUrl` pre-approval is enforced in Plan 02's token handler, not by exposing review state in the contract. This plan adds `approvedAt` so the gate is representable. |
| T-01-02 | Tampering | ConfirmMediaUploadRequest.capturedAt | accept | capturedAt is optional, nullable, ordering-only input; it grants no access and bypasses no tier cap. Validated as date-time by generated zod. Worker (Plan 02) only uses it for ordering with a createdAt fallback. |
| T-01-03 | Tampering | drizzle-kit enum migration | mitigate | Additive `ADD VALUE` only; no rename/remove of existing enum values (would break consumers). Verified by grep that original four values remain. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages added this phase (RESEARCH Package Legitimacy Audit: zero new deps). No install task exists; gate not triggered. |
</threat_model>

<verification>
- DB push applied the enum value and three nullable columns without error.
- Orval regenerated hooks (`useApproveEventVideo`, `useRegenerateEventVideo`) and zod; `pnpm run typecheck` passes.
- No file under `generated/` was hand-edited; `lib/api-zod/src/index.ts` unchanged.
</verification>

<success_criteria>
- `ready_for_review` is an additive enum value; `approved_at`, `superseded_at`, `captured_at` columns exist and are nullable.
- Both new endpoints exist in the spec with correct operationIds and security; generated hooks present.
- VideoJobStatus exposes `approvedAt`; ConfirmMediaUploadRequest accepts optional `capturedAt`.
- Workspace typechecks.
</success_criteria>

<output>
Create `.planning/phases/01-same-day-edit-video-control/01-01-SUMMARY.md` when done.
</output>
